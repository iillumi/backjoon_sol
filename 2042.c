// refer to https://blog.naver.com/ndb796/221282210534

#include <stdio.h>
	
int main () {
	int n,m,k,a,b,c;
	scanf("%d %d %d", &n, &m, &k);
	long long int arry[n+1];
	for (int i=1;i<=n;i++){
		scanf("%lld", &arry[i]);
	}
	
	for (int i=0;i<m+k;i++) {
		scanf("%d %d %d", &a, &b, &c);
		if(a==1) {
		}
		else {
		}
	}


	return 0;
}
